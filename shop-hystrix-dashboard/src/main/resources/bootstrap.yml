server:
  port: 9099

spring:
  application:
    name: shop-hystrix-dashboard
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info,hystrix.stream