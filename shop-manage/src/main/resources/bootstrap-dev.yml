spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848

security:
  oauth2:
    resource:
      id: shop-manage
      user-info-uri: http://localhost:8095/oauth/principal
      prefer-token-info: true
