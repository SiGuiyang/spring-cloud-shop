feign:
  sentinel:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info

server:
  port: 9988
spring:
  application:
    name: shop-job
  profiles:
    active: dev
